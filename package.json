{
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "start": "concurrently \"cd frontend && npm start\" \"cd backend && npm run backend\"",
    "inst": "concurrently \"cd frontend && npm i\" \"cd backend && npm i\"",
    "start:frontend": "cd frontend && npm start",
    "start:backend": "cd backend && npm run backend"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "react-to-print": "^2.15.1"
  }
}
